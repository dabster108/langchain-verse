# Datasets

Large fraud datasets live under `raw/` and should not be committed to git.

- `raw/`: original downloaded CSV files.
- `processed/`: cleaned, sampled, or feature-engineered files generated by scripts.

Current raw files are from the IEEE-CIS fraud detection dataset. Add PaySim files here when downloaded.
