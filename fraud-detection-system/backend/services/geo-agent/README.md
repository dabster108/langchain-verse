# Geo Agent

FastAPI microservice responsible for future location and Neo4j graph-context risk checks.

Run locally:

```bash
uv run python main.py
```

Health check:

```bash
curl http://localhost:8002/health
```
