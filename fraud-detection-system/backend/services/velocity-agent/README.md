# Velocity Agent

FastAPI microservice responsible for future transaction velocity risk checks.

Run locally:

```bash
uv run python main.py
```

Health check:

```bash
curl http://localhost:8001/health
```
